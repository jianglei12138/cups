service printer
{
	disable = yes
	socket_type = stream
	protocol = tcp
	wait = no
	user = system
	group = system
	passenv =
	server = /system/usr/root/lib/cups/daemon/cups-lpd
	server_args = -o document-format=application/octet-stream
}
